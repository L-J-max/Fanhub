# 2026-08-22 工作记录

## 上传 FanHub 到 GitHub
- 目标仓库：`L-J-max/Fanhub`（已存在，公开，默认分支 main，初始为空）。
- 本地初始化：在 `E:\Workbuddy_workspace\1` 执行 `git init -b main`，配置 user.name=L-J-max / user.email=fanhub@local，commit 65 个文件（cfda9df）。
- `.gitignore` 补充排除：`*.db-shm`、`*.db-wal`、`*.secret`、`deploy.log`（原有 /data、*.db、.next、node_modules、next-env.d.ts）。
- 推送方式：GitHub 连接器的 OAuth token 是加密存储，git CLI 无法读取；连接器 `create_or_update_file` 写仓库返回 403（integration 无写权限）。最终用户提供了个人 PAT（ghp_...），用 `git push -u origin main` 一次性推送成功。PAT 仅在当次命令 URL 内联使用，未持久化到 git config/credential store；推送后已 `git remote set-url` 重置为无 token 的干净地址。
- 验证：远程 main commit = cfda9df，文件数 65，敏感/运行时文件（.secret、app.db*、deploy.log、.next、node_modules）均未上传。
- 教训：GitHub 连接器（应用级 integration 授权）默认只读/受限，无法代写个人仓库；上传私人仓库必须用用户自己的 PAT。
